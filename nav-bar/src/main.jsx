import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";

import App from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <BrowserRouter>
    <center>
      <App w='180px' h='80px' bg='red.200'/>
    </center>
  </BrowserRouter>
);
cd;
